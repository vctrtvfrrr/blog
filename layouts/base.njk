{%- set pageTitle = sitename -%}

{# Liberar quando o site tiver mais páginas além do blog
{%- if collections[collection].metadata.title -%}
  {%- set pageTitle = collections[collection].metadata.title + ' | ' + pageTitle -%}
{%- endif -%} #}

{%- if title -%}
  {%- set pageTitle = title + ' &mdash; ' + pageTitle -%}
{%- endif -%}

{%- if excerpt -%}
  {%- set pageDescription = excerpt -%}
{%- elseif description -%}
  {%- set pageDescription = description -%}
{%- else -%}
  {%- set pageDescription = author -%}
{%- endif -%}

{%- if keywords -%}
  {%- set pageKeywords = keywords -%}
  {% if tags %}
    {%- set pageKeywords = pageKeywords + ', ' + tags -%}
  {% endif %}
{%- endif -%}
<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01 Transitional//EN" "http://www.w3.org/TR/html4/loose.dtd">
<HTML>
	<HEAD>
		{% include 'partials/head.njk' %}
		{% block head %}{% endblock head %}
	</HEAD>
	<BODY>
		<table width="768" border="0" cellspacing="0" cellpadding="10">
			{% include 'partials/header.njk' %}
			<tr>
				<td valign="top">
					{% block content %}
					{{ contents | safe }}
					{% endblock %}
				</td>
				<td width="220" valign="top">
					{% include 'partials/sidebar.njk' %}
				</td>
			</tr>
			{% include 'partials/footer.njk' %}
		</table>

	<script language="JavaScript"><!--
	window.defaultStatus=" - = {{ sitename | safe }} = -  ";

	function click() {
	if (event.button==2) {
	alert("{{ sitename | safe }}")
	alert('Se quiser alguma coisa me mande um email!')
	}

	}
	document.onmousedown=click
	// --></script>
	</BODY>
</HTML>